# Core deep learning
# NOTE: for GPU, install the CUDA build of torch first (see README "Setup").
# Tested with torch 2.11.0+cu128 / torchvision 0.26.0+cu128 (CUDA 12.8) on Python 3.14.
torch>=2.2
torchvision>=0.17
timm>=1.0.0            # DINO / DINOv2 ViT backbones

# Scientific stack
numpy>=1.24
scikit-learn>=1.3     # AUROC, F1, precision/recall
matplotlib>=3.7       # plots & heatmaps
Pillow>=9.0           # image loading
tqdm>=4.60            # progress bars

# Notebook (optional — only needed to run Final_Code.ipynb)
jupyter
nbconvert
nbformat
